# {{contractName}} — Anchor Counter

Add to your Anchor workspace `programs/` folder or run `anchor build` from project root.

Update `declare_id!` and `Anchor.toml` program id after `anchor keys sync`.
