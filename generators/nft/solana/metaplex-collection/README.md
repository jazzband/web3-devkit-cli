# {{contractName}} — Metaplex Collection

Scripts to create an NFT collection on Solana. Install Metaplex SDK dependencies before running.

```bash
npm install @metaplex-foundation/mpl-token-metadata @solana/web3.js
npx ts-node scripts/create-collection.ts
```
