# WalletConnect integration

Generated by `web3 add wallet-connect`.

Adds WalletConnect alongside injected wallets. Requires `NEXT_PUBLIC_WALLET_CONNECT_PROJECT_ID`.

See `components/web3/connect-button.tsx` for browser + WalletConnect options.
