# {{projectName}}

Next.js + **Solana wallet adapter** frontend scaffold via web3-devkit.

## Quick start

```bash
cd frontend
npm install
npm run dev
```

Set `NEXT_PUBLIC_SOLANA_RPC` in `.env` (defaults to devnet in scaffold).
