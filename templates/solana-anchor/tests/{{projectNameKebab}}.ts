/**
 * Example Anchor test — run with `anchor test`
 * Requires full Anchor workspace setup (yarn install, etc.)
 */
import * as anchor from "@coral-xyz/anchor";

describe("{{projectNameKebab}}", () => {
  it("is scaffolded", () => {
    anchor.setProvider(anchor.AnchorProvider.env());
    // Add program tests after `anchor init` dependencies are installed
  });
});
