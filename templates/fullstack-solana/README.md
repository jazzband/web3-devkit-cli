# {{projectName}}

Full-stack **Solana** project: Anchor program + Next.js wallet UI + API.

## Quick start

```bash
anchor build && anchor test
cd frontend && npm install && npm run dev
cd backend/api && npm run dev
```

Configure `ANCHOR_WALLET` and `SOLANA_RPC_URL` in `.env`.
